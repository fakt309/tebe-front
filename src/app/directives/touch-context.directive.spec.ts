import { TouchContextDirective } from './touch-context.directive';

describe('TouchContextDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchContextDirective();
    expect(directive).toBeTruthy();
  });
});
